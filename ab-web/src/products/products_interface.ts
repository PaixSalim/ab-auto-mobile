export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum ProductState {
  NEW = 'new',
  OLD = 'old',
}

export enum ValidationStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}
